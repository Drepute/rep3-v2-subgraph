type Pod @entity {
  id: Bytes!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  transactionHash: Bytes!
  credentials: [Credential!]! @derivedFrom(field: "parentPod")
}

type Credential @entity {
  id: Bytes!
  parentPod: Pod!
  facets: [Bytes!]!
}
